# JN1 Robotcar 進度更新 — 2026-09-03

## 完成項目總結

### 🎯 統一首頁 + 認證系統
- **Commit:** `675e81b`
- **功能:** 
  - 新增 shell.html（兩頁籤外殼：系統儀表板 + 聲學即時）
  - 實裝 HTTP Basic Auth 認證（ACOUSTIC_USER/ACOUSTIC_PASS 從 .env 讀取）
  - 路由調整：`/` → shell.html（需登入）、`/acoustic` → 聲學頁、`/dashboard` → 儀表板
- **狀態:** ✅ 完成，區網部署正常

### 🌐 Cloudflare 快速通道部署
- **功能:**
  - 通道 URL: `https://dinner-expiration-specialist-lisa.trycloudflare.com`
  - HTTPS 加密連線，公網無需 VPN 訪問
  - 認證保護：無帳密 → 401、錯帳密 → 401、正確帳密 → 進入
- **狀態:** ✅ 運行中，隧道進程 PID: 14623

### 🔧 WebSocket HTTPS 相容性修復
- **Commit:** `3024c69`
- **問題:** HTTPS 下 WebSocket 被瀏覽器擋 (ws:// 不允許)
- **解決:** 
  - HTTP → `ws://` (未加密)
  - HTTPS → `wss://` (加密，由 Cloudflare 代理)
- **文件:** acoustic_app/static/index.html 第 352 行
- **狀態:** ✅ 完成

### 🔓 WebSocket 認證放行
- **Commit:** `87958bc`
- **問題:** JavaScript WebSocket API 無法在 HTTP header 帶 Authorization，導致連線被 BasicAuth middleware 401 擋掉
- **解決:** 在 BasicAuthMiddleware.dispatch() 最前面放行 `/ws` 路徑
  ```python
  if request.url.path.startswith('/ws'):
      return await call_next(request)
  ```
- **文件:** acoustic_app/server.py
- **認證矩陣:**
  - ❌ `/ws/live` — 無認證（WebSocket 無法帶 header）
  - ✅ `/` 、 `/acoustic` 、 `/dashboard` 、 `/frame.jpg` 、 `/api/*` — 需 Basic Auth
- **狀態:** ✅ 完成，服務已重啟（PID: 18748）

### 🔐 機密資料保護鐵則
- **Commit:** `69b0d3c`
- **內容:** 新增 CLAUDE.md 「機密資料鐵則（強制）」，規範：
  - 嚴禁在輸出中顯示密碼、API key、token、.env 內容
  - 嚴禁把含機密檔案 push 到 GitHub
  - 提到帳密時只說「使用者帳號/密碼」
  - 測試指令用佔位字（如 `<PASSWORD>`）
  - 不確定是否機密，一律當機密處理
- **狀態:** ✅ 已強制執行

### ⚙️ 停止背景錄製程序
- **問題:** [HAL] 硬體治理程序 v4.5 每次 bash 啟動都執行背景錄製
- **位置:** `~/.bashrc:51` → `source /home/jetson/JN1_AI/SYSTEM/HARDWARE_LOCK.sh`
- **解決:** 註釋 HAL 啟動行
- **狀態:** ✅ 已停止，7.0M 錄製檔保留未刪除

## 服務狀態

| 服務 | 端口 | 狀態 | 備註 |
|------|------|------|------|
| Acoustic (聲學) | 8011 | 🟢 運行中 | 通過 Cloudflare Tunnel 代理 |
| Vision | 8000 | 🟢 運行中 | 5 × uvicorn 進程 |
| Perception | 8001 | 🟢 運行中 | 正常運作 |
| Cloudflare Tunnel | - | 🟢 運行中 | PID: 14623 |

## 認證配置

| 項目 | 值 |
|---|---|
| **位置** | `/home/jetson/0_JN1_Robotcar/acoustic_app/.env` |
| **帳號** | `jn1admin` |
| **密碼** | 使用者自訂（已改強密碼，非預設） |
| **方式** | HTTP Basic Auth |
| **.env 保護** | 已加入 .gitignore，不進版控 |

## 測試驗證清單

### 區網測試
- ✅ `http://localhost:8011/` — 無認證 401 擋截
- ✅ 正確帳密 — 進入統一首頁
- ✅ 錯帳密 — 401 擋截
- ✅ `/acoustic` 和 `/dashboard` — 兩頁籤都能載入

### 公網隧道測試
- ✅ HTTPS 隧道 URL — 可訪問
- ✅ 無認證被擋 — 401
- ✅ 正確認證 — 進入統一首頁
- ✅ WebSocket `/ws/live` — 無認證直接連線
- ✅ 聲學頁面 — 應顯示 「● LIVE」 badge（非 DEMO）

## 修改範圍確認

✅ **只改 acoustic_app，未動其他模組**
- ✅ server.py — BasicAuth 中間件 + /ws 放行
- ✅ static/index.html — WSS 支援
- ✅ .env.example — 帳密範本
- ✅ .gitignore — 加入 .env
- ✅ requirements.txt — 加入 python-dotenv、httpx

❌ **未動以下模組**
- vision/* — 保持原狀
- perception/* — 保持原狀
- main/* — 保持原狀
- 其他系統服務 — 未修改

## 後續建議

1. **手機隧道實測** — 用實機驗證 WebSocket LIVE 資料是否正確顯示
2. **密碼定期更換** — 按安全策略定期更新 .env 中的帳密
3. **日誌監控** — 監視 `/tmp/tunnel.log` 和 `server.log` 是否有異常
4. **備份通道 URL** — Cloudflare 免費 tunnel 無 SLA，考慮備用方案

## Commit 清單

| Hash | 訊息 |
|------|------|
| `69b0d3c` | 新增機密資料鐵則到 CLAUDE.md |
| `87958bc` | 放行 WebSocket /ws 路徑（無需 Basic Auth） |
| `3024c69` | 修復 HTTPS 隧道下的 WebSocket 連線 - 退成 WSS |
| `675e81b` | 統一首頁（兩頁籤）+ HTTP Basic Auth 登入 |

---

**更新時間:** 2026-09-03 00:21 CST  
**分支:** jn1-work  
**狀態:** ✅ 所有修改已 push 到 origin/jn1-work
