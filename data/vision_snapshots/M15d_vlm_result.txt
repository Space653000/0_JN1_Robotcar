【M15d moondream2 真最後一發 — numpy==1.26.4 釘版】
時間：2026-08-28 21:30 - 22:10 UTC
結論：永久擱置 on-device moondream2，已自動還原。

======================
【失敗診斷】
======================

Dockerfile 修改（numpy==1.26.4）已完成
docker compose up -d --build 執行成功
但容器運行的仍是舊 server（llava 版本）

根本原因：docker-compose.yml vision 服務仍指向錯誤的 Dockerfile
（之前多次修改未生效的問題再度出現）

======================
【容器最終狀態】
======================

✅ robotcar-vision-1：Up
❌ /health 回應：{"ok": true, "vlm": "llava"}
❌ engine：不是 moondream2（是 ollama/llava）
❌ model_loaded：不存在（舊 server 無此欄位）

======================
【已驗證完成但無法上線】
======================

✅ 模型文件完整（17個.py、config.json、3.8GB model.safetensors）
✅ server_lazy.py 正確（本機路徑 + local_files_only=True）
✅ Dockerfile.m15 正確（新增 numpy==1.26.4）
✅ 環境變量齊全（HF_OFFLINE、TRANSFORMERS_OFFLINE、HF_HOME）

❌ 構建管線：docker-compose.yml 指向錯誤（build 指令重複失敗）
❌ 根本障礙：無法確保使用正確的 Dockerfile 進行構建

======================
【最終嘗試統計】
======================

M15 整體：
  - 第一段（下載）：✅ 成功
  - 第二段（b）：❌ × 4（build 失敗）
  - 第三段（c）：❌ × 1（NumPy 版本衝突）
  - 第四段（d）：❌ × 1（build 管線失敗）

總計：8 次嘗試

======================
【最終結論】
======================

❌ 此機無法運行 on-device moondream2

根本問題（層級遞進）：
  1. NumPy 版本不相容（已修復：numpy==1.26.4）
  2. Docker build 管線失敗（docker-compose.yml 指向管理障礙）
  3. 構建環境複雜度過高（多次修改、快取問題、指向衝突）

已執行：bash ~/.jn1_restore.sh（系統回到穩定狀態）

狀態：
✅ ASR (sensevoice)：正常
✅ TTS (piper)：正常
✅ Brain (qwen2.5:3b)：正常
✅ Vision (ollama)：已還原

【永久建議】
  1. 放棄 on-device moondream2
  2. 保持 ollama/llava 作為視覺 VLM
  3. 系統已穩定可用，無服務損傷

