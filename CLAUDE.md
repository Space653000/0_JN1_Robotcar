# 語言規則

所有輸出（終端訊息、程式碼註解、commit 訊息、報告）一律只用繁體中文（台灣）或英文，禁止使用簡體中文、日文、韓文或其他語言。

## 機密資料鐵則（強制）

- 嚴禁在任何輸出（終端訊息、報告、程式碼、commit 訊息、log）中印出、echo、cat、或以任何方式顯示密碼、帳號密碼、API key、token 或 .env 的內容。
- 嚴禁把 .env 或任何含機密的檔案 commit / push 到 GitHub。
- 需要提到帳密時，一律只說「使用者的帳號/密碼」，絕不寫出實際值。
- 產生測試指令時，用佔位字（如 `<PASSWORD>`），不得填入真實密碼。
- 若不確定某值是否為機密，一律當機密處理、不顯示。
